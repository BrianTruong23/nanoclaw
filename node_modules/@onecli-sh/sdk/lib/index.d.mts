interface OneCLIOptions {
    apiKey?: string;
    url?: string;
    timeout?: number;
}

interface ContainerConfig {
    env: Record<string, string>;
    caCertificate: string;
    caCertificateContainerPath: string;
}
interface ApplyContainerConfigOptions {
    combineCaBundle?: boolean;
    addHostMapping?: boolean;
    agent?: string;
}

interface CreateAgentInput {
    name: string;
    identifier: string;
}
interface CreateAgentResponse {
    id: string;
    name: string;
    identifier: string;
    createdAt: string;
}
interface EnsureAgentResponse {
    name: string;
    identifier: string;
    created: boolean;
}

declare class OneCLI {
    private containerClient;
    private agentsClient;
    constructor(options?: OneCLIOptions);
    getContainerConfig: (agent?: string) => Promise<ContainerConfig>;
    applyContainerConfig: (args: string[], options?: ApplyContainerConfigOptions) => Promise<boolean>;
    createAgent: (input: CreateAgentInput) => Promise<CreateAgentResponse>;
    ensureAgent: (input: CreateAgentInput) => Promise<EnsureAgentResponse>;
}

declare class ContainerClient {
    private baseUrl;
    private apiKey;
    private timeout;
    constructor(baseUrl: string, apiKey: string, timeout: number);
    getContainerConfig: (agent?: string) => Promise<ContainerConfig>;
    applyContainerConfig: (args: string[], options?: ApplyContainerConfigOptions) => Promise<boolean>;
}

declare class AgentsClient {
    private baseUrl;
    private apiKey;
    private timeout;
    constructor(baseUrl: string, apiKey: string, timeout: number);
    createAgent: (input: CreateAgentInput) => Promise<CreateAgentResponse>;
    ensureAgent: (input: CreateAgentInput) => Promise<EnsureAgentResponse>;
}

declare class OneCLIError extends Error {
    constructor(message: string);
}
declare class OneCLIRequestError extends Error {
    readonly url: string;
    readonly statusCode: number;
    constructor(message: string, requestData: {
        url: string;
        statusCode: number;
    });
}

export { AgentsClient, type ApplyContainerConfigOptions, ContainerClient, type ContainerConfig, type CreateAgentInput, type CreateAgentResponse, type EnsureAgentResponse, OneCLI, OneCLIError, type OneCLIOptions, OneCLIRequestError };
